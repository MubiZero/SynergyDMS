# Synergy DMS — Система Управления Документооборотом Университета

<p align="center">
  <img src="https://img.shields.io/badge/Backend-Go%201.21-00ADD8?style=for-the-badge&logo=go" alt="Go">
  <img src="https://img.shields.io/badge/Frontend-Flutter%203.5-02569B?style=for-the-badge&logo=flutter" alt="Flutter">
  <img src="https://img.shields.io/badge/Database-PostgreSQL%2015-336791?style=for-the-badge&logo=postgresql" alt="PostgreSQL">
  <img src="https://img.shields.io/badge/Container-Docker-2496ED?style=for-the-badge&logo=docker" alt="Docker">
</p>

## Описание проекта

**Synergy DMS** — это современная автоматизированная система управления документооборотом, разработанная для высших учебных заведений. Система обеспечивает эффективный обмен документами между студентами и административным персоналом с поддержкой рабочих процессов согласования, ведением аудита и прикреплением файлов.

### Цель разработки

Автоматизация процессов документооборота в университете с целью:
- Сокращения времени обработки заявок и документов
- Повышения прозрачности административных процедур
- Обеспечения контроля и аудита всех операций с документами
- Упрощения взаимодействия студентов и сотрудников университета

---

## Архитектура системы

### Общая схема

```
┌─────────────────┐      HTTPS/REST API     ┌─────────────────┐
│                 │ ◄──────────────────────►│                 │
│  Flutter App    │                         │   Go Backend    │
│  (Клиент)       │                         │   (Fiber API)   │
│                 │                         │                 │
└─────────────────┘                         └────────┬────────┘
                                                     │
                                                     │ GORM
                                                     ▼
                                            ┌─────────────────┐
                                            │                 │
                                            │   PostgreSQL    │
                                            │   (Database)    │
                                            │                 │
                                            └─────────────────┘
```

### Технологический стек

| Компонент | Технология | Назначение |
|-----------|------------|------------|
| **Backend** | Go 1.21 + Fiber v2 | REST API сервер, высокая производительность |
| **ORM** | GORM | Работа с базой данных, миграции |
| **Аутентификация** | JWT (JSON Web Tokens) | Безопасная авторизация пользователей |
| **База данных** | PostgreSQL 15 | Хранение данных, ACID-транзакции |
| **Frontend** | Flutter 3.5 | Кроссплатформенный клиент (Web, Mobile) |
| **State Management** | GetX | Управление состоянием, навигация |
| **HTTP клиент** | Dio | Сетевые запросы с перехватчиками |
| **Контейнеризация** | Docker + Docker Compose | Развёртывание и масштабирование |

---

## Структура проекта

```
synergy_dms/
├── docker-compose.yml          # Конфигурация контейнеров
├── README.md                   # Документация
│
├── backend/                    # Серверная часть (Go)
│   ├── Dockerfile              # Сборка контейнера
│   ├── go.mod                  # Зависимости Go
│   ├── go.sum                  # Контрольные суммы
│   ├── main.go                 # Точка входа
│   │
│   ├── config/
│   │   └── config.go           # Конфигурация из ENV
│   │
│   ├── models/
│   │   ├── database.go         # Подключение к БД, миграции
│   │   ├── user.go             # Модель пользователя
│   │   ├── document.go         # Модель документа
│   │   └── history.go          # Модель истории (аудит)
│   │
│   ├── handlers/
│   │   ├── auth.go             # Регистрация, вход, профиль
│   │   ├── user.go             # Управление пользователями
│   │   ├── document.go         # CRUD документов
│   │   └── upload.go           # Загрузка файлов
│   │
│   ├── middleware/
│   │   └── auth.go             # JWT аутентификация, RBAC
│   │
│   ├── services/
│   │   └── expiration.go       # Фоновая служба истечения срока
│   │
│   └── uploads/                # Хранилище файлов
│
└── frontend/                   # Клиентская часть (Flutter)
    ├── pubspec.yaml            # Зависимости Flutter
    │
    └── lib/
        ├── main.dart           # Точка входа, маршрутизация
        │
        ├── controllers/
        │   ├── auth_controller.dart     # Аутентификация
        │   ├── document_controller.dart # Документы
        │   └── user_controller.dart     # Пользователи
        │
        ├── models/
        │   ├── user.dart       # Модель пользователя
        │   ├── document.dart   # Модель документа
        │   └── history.dart    # Модель истории
        │
        ├── screens/
        │   ├── login_screen.dart         # Вход
        │   ├── register_screen.dart      # Регистрация
        │   ├── dashboard_screen.dart     # Главный экран
        │   ├── create_document_screen.dart # Создание документа
        │   └── document_detail_screen.dart # Детали документа
        │
        ├── services/
        │   └── api_service.dart  # HTTP клиент
        │
        ├── utils/
        │   ├── constants.dart    # Константы
        │   └── theme.dart        # Тема оформления
        │
        └── widgets/
            ├── document_card.dart      # Карточка документа
            └── pending_admins_tab.dart # Вкладка заявок
```

---

## Модель пользователей и ролей

### Роли пользователей

| Роль | Описание | Права доступа |
|------|----------|---------------|
| **Студент** | Обучающийся университета | Создание документов, просмотр своих документов |
| **Администратор** | Сотрудник деканата/кафедры | Одобрение/отклонение документов, делегирование |
| **Супер-администратор** | Главный администратор | Все права + управление администраторами |

### Схема регистрации

```
Студент ────► Автоматическое одобрение ────► Полный доступ

Администратор ────► Ожидание одобрения ────► Одобрение супер-админом ────► Полный доступ
```

---

## Жизненный цикл документа

### Статусы документов

```
                    ┌─────────────┐
                    │   Создан    │
                    │  (Pending)  │
                    └──────┬──────┘
                           │
           ┌───────────────┼───────────────┐
           ▼               ▼               ▼
    ┌─────────────┐ ┌─────────────┐ ┌─────────────┐
    │  Одобрен    │ │  Отклонён   │ │   Истёк     │
    │ (Approved)  │ │ (Rejected)  │ │  (Expired)  │
    └─────────────┘ └─────────────┘ └─────────────┘
```

### Приоритеты

| Уровень | Цвет | Описание |
|---------|------|----------|
| 🟢 Низкий | Зелёный | Стандартные заявки |
| 🟡 Средний | Жёлтый | Требует внимания |
| 🔴 Высокий | Красный | Срочные документы |

---

## API Endpoints

### Аутентификация

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `POST` | `/auth/register` | Регистрация пользователя |
| `POST` | `/auth/login` | Вход в систему |
| `GET` | `/auth/profile` | Получение профиля (требует JWT) |

### Управление пользователями

| Метод | Endpoint | Описание | Доступ |
|-------|----------|----------|--------|
| `GET` | `/users/pending-admins` | Список заявок на роль админа | Супер-админ |
| `PUT` | `/users/:id/approve` | Одобрение администратора | Супер-админ |
| `GET` | `/users/admins` | Список администраторов | Админ+ |

### Документы

| Метод | Endpoint | Описание | Доступ |
|-------|----------|----------|--------|
| `GET` | `/documents` | Список документов | Авторизованный |
| `POST` | `/documents` | Создание документа | Студент+ |
| `GET` | `/documents/:id` | Детали документа | Авторизованный |
| `PUT` | `/documents/:id/status` | Изменение статуса | Админ+ |
| `PUT` | `/documents/:id/delegate` | Делегирование | Админ+ |
| `GET` | `/documents/:id/history` | История изменений | Авторизованный |

### Загрузка файлов

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `POST` | `/api/upload` | Загрузка файла (multipart/form-data) |
| `GET` | `/uploads/:filename` | Скачивание файла |

---

## Развёртывание

### Требования

- Docker 20.10+
- Docker Compose 2.0+
- Go 1.21+ (для локальной разработки)
- Flutter 3.5+ (для мобильной разработки)

### Быстрый старт (Docker)

```bash
# Клонирование репозитория
git clone <repository-url>
cd synergy_dms

# Запуск контейнеров
docker compose up -d

# Проверка статуса
docker compose ps

# Просмотр логов
docker compose logs -f backend
```

### Локальная разработка

```bash
# Запуск только базы данных
docker compose up -d postgres

# Запуск backend локально
cd backend
go mod download
go run main.go

# Запуск frontend
cd frontend
flutter pub get
flutter run -d chrome
```

---

## Конфигурация

### Переменные окружения (Backend)

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `DB_HOST` | Хост PostgreSQL | `postgres` |
| `DB_PORT` | Порт PostgreSQL | `5432` |
| `DB_USER` | Пользователь БД | `synergy_user` |
| `DB_PASSWORD` | Пароль БД | `synergy_pass_2024` |
| `DB_NAME` | Имя базы данных | `synergy_dms` |
| `JWT_SECRET` | Секрет для JWT | `synergy_jwt_secret_key_2024` |
| `SERVER_PORT` | Порт сервера | `8080` |

### Конфигурация Frontend

Файл: `lib/utils/constants.dart`

```dart
// Для Android эмулятора
static const String baseUrl = 'http://10.0.2.2:8080';

// Для iOS симулятора / Web
static const String baseUrl = 'http://localhost:8080';

// Для production
static const String baseUrl = 'http://YOUR_SERVER_IP:8080';
```

---

## Безопасность

### Реализованные меры

1. **JWT аутентификация** — токены с ограниченным сроком действия
2. **Хеширование паролей** — bcrypt с автоматическим salt
3. **RBAC** — контроль доступа на основе ролей
4. **CORS** — защита от межсайтовых запросов
5. **Валидация ввода** — проверка всех входящих данных
6. **Безопасное хранение** — flutter_secure_storage для токенов

### Учётные данные по умолчанию

| Роль | Email | Пароль |
|------|-------|--------|
| Супер-администратор | `super@synergy.ru` | `123456` |

>  **Важно:** Смените пароль супер-администратора перед развёртыванием в production!

---

## Сборка мобильного приложения

### Android APK

```bash
cd frontend

# Debug версия
flutter build apk --debug

# Release версия
flutter build apk --release
```

APK будет в: `build/app/outputs/flutter-apk/app-release.apk`

### iOS (требуется macOS)

```bash
flutter build ios --release
```

---

## Тестирование API

### Проверка здоровья сервера

```bash
curl http://localhost:8080/health
```

Ожидаемый ответ:
```json
{
  "status": "healthy",
  "service": "Synergy DMS",
  "version": "1.0.0"
}
```

### Регистрация пользователя

```bash
curl -X POST http://localhost:8080/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "student@synergy.ru",
    "password": "password123",
    "full_name": "Иванов Иван",
    "role": "student",
    "faculty": "Факультет информатики"
  }'
```

### Вход в систему

```bash
curl -X POST http://localhost:8080/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "super@synergy.ru",
    "password": "123456"
  }'
```

---

## Схема базы данных

### Таблица users

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL | Первичный ключ |
| email | VARCHAR(255) | Email (уникальный) |
| password | VARCHAR(255) | Хеш пароля |
| full_name | VARCHAR(255) | ФИО |
| role | VARCHAR(50) | Роль (student/admin/super_admin) |
| faculty | VARCHAR(255) | Факультет |
| is_approved | BOOLEAN | Статус одобрения |
| created_at | TIMESTAMP | Дата создания |
| updated_at | TIMESTAMP | Дата обновления |

### Таблица documents

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL | Первичный ключ |
| title | VARCHAR(255) | Название |
| description | TEXT | Описание |
| file_path | VARCHAR(500) | Путь к файлу |
| priority | INTEGER | Приоритет (1-3) |
| status | VARCHAR(50) | Статус документа |
| rejection_reason | TEXT | Причина отклонения |
| deadline | TIMESTAMP | Срок |
| creator_id | INTEGER | FK на users |
| assigned_to_id | INTEGER | FK на users (nullable) |
| created_at | TIMESTAMP | Дата создания |
| updated_at | TIMESTAMP | Дата обновления |

### Таблица histories

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL | Первичный ключ |
| document_id | INTEGER | FK на documents |
| actor_id | INTEGER | FK на users |
| action | VARCHAR(100) | Действие |
| comment | TEXT | Комментарий |
| timestamp | TIMESTAMP | Время действия |

---

## Фоновые процессы

### Служба автоматического истечения

Система автоматически проверяет документы каждый час и устанавливает статус "Истёк" для документов со статусом "На рассмотрении", созданных более 7 дней назад.

```go
// services/expiration.go
func StartExpirationService(db *gorm.DB) {
    ticker := time.NewTicker(1 * time.Hour)
    for range ticker.C {
        // Поиск и обновление просроченных документов
    }
}
```

---

## Возможности для расширения

1. **Уведомления** — Email/Push уведомления о изменении статуса
2. **Электронная подпись** — Интеграция с ЭЦП
3. **Шаблоны документов** — Предустановленные формы заявлений
4. **Статистика и аналитика** — Дашборды с графиками
5. **Интеграция с 1С** — Синхронизация с учётной системой
6. **QR-коды** — Быстрый доступ к документам

---

## Автор

**Мухамедов М.М. для ВКР**  
Тема: «Автоматизация документооборота высшего учебного заведения»

---

## Лицензия

MIT License

---

<p align="center">
  <b>Synergy DMS</b> — Современное решение для университетского документооборота
</p>
